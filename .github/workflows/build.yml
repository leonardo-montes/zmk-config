name: Build totem config
run-name: Building ZMK totem firmware

on:
  workflow_dispatch:
  push:
  pull_request:

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
